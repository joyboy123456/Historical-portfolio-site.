# React 管理后台使用指南

## 🚀 快速开始

### 启动开发服务器

```bash
cd portfolio-admin
npm run dev
```

访问地址：**http://localhost:5173/**

---

## ✨ 已实现功能

### 1. 作品管理
- ✅ 作品列表展示（卡片网格布局）
- ✅ 创建新作品
- ✅ 编辑作品信息
- ✅ 删除作品
- ✅ 图片上传（集成 Supabase `image-upload` API）
- ✅ 标签管理（添加/删除标签）
- ✅ 精选作品标记
- ✅ 显示顺序控制
- ✅ 表单验证（Zod + React Hook Form）

### 2. 简历管理
- ✅ 简历内容列表展示
- ✅ 创建新简历部分
- ✅ 编辑简历内容
- ✅ 区域类型管理（hero, about, skills, contact 等）
- ✅ 显示顺序控制
- ✅ 表单验证

### 3. UI 组件
- ✅ 响应式布局（Tailwind CSS）
- ✅ 导航栏和路由（React Router v6）
- ✅ 模态框对话框（Radix UI Dialog）
- ✅ 按钮、输入框、文本域
- ✅ 复选框、标签
- ✅ 图片上传组件
- ✅ 错误边界

### 4. 数据库优化
- ✅ 索引优化（display_order, category, featured, section_type）
- ✅ 自动更新 updated_at 触发器
- ✅ RLS 安全策略（公开读取，认证用户完全访问）
- ✅ 字段约束和验证

---

## 📁 项目结构

```
portfolio-admin/
├── src/
│   ├── components/           # React 组件
│   │   ├── ui/               # UI 基础组件（Button, Dialog, Input 等）
│   │   ├── Layout.tsx        # 主布局组件
│   │   ├── ProjectForm.tsx   # 作品表单
│   │   ├── ResumeForm.tsx    # 简历表单
│   │   ├── ImageUpload.tsx   # 图片上传组件
│   │   └── ErrorBoundary.tsx # 错误边界
│   ├── pages/                # 页面组件
│   │   ├── ProjectsPage.tsx  # 作品管理页面
│   │   └── ResumePage.tsx    # 简历管理页面
│   ├── lib/                  # 工具库
│   │   ├── supabase.ts       # Supabase 客户端配置
│   │   └── utils.ts          # 工具函数（cn）
│   ├── App.tsx               # 主应用组件
│   └── main.tsx              # 入口文件
├── package.json              # 依赖配置
├── vite.config.ts            # Vite 构建配置
├── tailwind.config.js        # Tailwind CSS 配置
└── tsconfig.json             # TypeScript 配置
```

---

## 🎨 技术栈

- **框架**: React 18 + TypeScript
- **构建工具**: Vite 6
- **样式方案**: Tailwind CSS + PostCSS
- **UI 组件库**: Radix UI（20+ 组件）
- **表单管理**: React Hook Form + Zod
- **路由**: React Router DOM v6
- **数据库客户端**: Supabase JS SDK
- **状态与通知**: Sonner（Toast 通知）

---

## 🔧 API 集成

### 作品 API (`projects-api`)
- `GET /projects-api` - 获取所有作品
- `GET /projects-api/:id` - 获取单个作品
- `POST /projects-api` - 创建作品
- `PUT /projects-api/:id` - 更新作品
- `DELETE /projects-api/:id` - 删除作品

### 简历 API (`resume-api`)
- `GET /resume-api` - 获取所有简历部分
- `GET /resume-api/:id` - 获取单个简历部分
- `POST /resume-api` - 创建简历部分
- `PUT /resume-api/:id` - 更新简历部分
- `DELETE /resume-api/:id` - 删除简历部分

### 图片上传 API (`image-upload`)
- `POST /image-upload` - 上传图片到 Supabase Storage
- 支持格式：JPG, PNG, WebP
- 大小限制：10MB

---

## 📊 数据库配置

运行数据库迁移脚本以优化性能和安全性：

```bash
# 在 Supabase SQL Editor 中运行
supabase/tables/migrations.sql
```

**包含内容**:
- 索引优化（提升查询性能）
- 自动更新 `updated_at` 的触发器
- Row Level Security (RLS) 策略
- 字段约束和验证

---

## 🌐 部署

### 构建生产版本

```bash
cd portfolio-admin
npm run build
```

构建产物在 `dist/` 目录。

### 部署选项

1. **Vercel / Netlify**
   - 自动检测 Vite 项目
   - 构建命令：`npm run build`
   - 输出目录：`dist`

2. **静态服务器**
   ```bash
   npm run preview  # 本地预览生产构建
   ```

3. **与前端网站集成**
   - 可以将构建产物部署到 `/admin` 子路径
   - 配置 Vite base 路径

---

## 🎯 后续优化建议

### P0 - 紧急
1. **添加身份认证**
   - 集成 Supabase Auth
   - 登录/登出功能
   - 受保护的路由

### P1 - 重要
2. **拖拽排序**
   - 使用 `@dnd-kit/core` 实现拖拽
   - 批量更新 display_order

3. **搜索和筛选**
   - 按标题搜索
   - 按分类筛选
   - 按精选状态筛选

4. **富文本编辑器**
   - 简历内容使用 Markdown 或富文本编辑
   - 集成 TipTap 或 Slate

### P2 - 优化
5. **数据统计面板**
   - 作品数量统计
   - 最近更新的作品
   - 使用 Recharts 可视化

6. **批量操作**
   - 批量删除
   - 批量更新分类
   - 批量设置精选

7. **操作日志**
   - 记录创建/更新/删除操作
   - 显示操作历史

---

## 🐛 故障排查

### 问题：无法连接到 Supabase
**解决方案**:
- 检查 `src/lib/supabase.ts` 中的配置
- 确认 Supabase URL 和 Anon Key 正确
- 检查网络连接

### 问题：图片上传失败
**解决方案**:
- 确认 Supabase Storage 存储桶 `portfolio-images` 已创建
- 检查存储桶权限设置（应该允许公开访问）
- 检查图片格式和大小限制

### 问题：样式不显示
**解决方案**:
```bash
# 重新安装依赖
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

---

## 📝 开发说明

### 添加新页面
1. 在 `src/pages/` 创建新组件
2. 在 `src/App.tsx` 添加路由
3. 在 `src/components/Layout.tsx` 添加导航链接

### 添加新 UI 组件
- 参考现有 UI 组件风格
- 使用 Radix UI 原语
- 遵循 Tailwind CSS 命名约定
- 保持 Editorial 极简设计风格

### 代码规范
- TypeScript 严格模式
- ESLint 代码检查
- 组件使用函数式组件 + Hooks
- 表单使用 React Hook Form + Zod

---

## 📞 联系支持

如有问题或建议，请查看：
- 前端网站文档：`portfolio-website/CMS_DOCUMENTATION.md`
- 设计规范：`docs/design-specification.md`
- 测试清单：`TESTING_CHECKLIST.md`

---

**生成时间**: 2025-11-02
**版本**: 1.0.0
**维护者**: Claude AI
